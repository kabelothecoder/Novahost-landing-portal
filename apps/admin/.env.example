# The admin console talks to the same Supabase project as everything else.
# These are the publishable (anon) credentials — safe in a browser bundle.
# Authority comes from the signed-in account being in `public.admin_users`,
# never from this key.
VITE_SUPABASE_URL=https://epulmnfbxjmaimefhofp.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=
